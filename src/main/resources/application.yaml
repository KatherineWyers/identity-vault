spring:
  application:
    name: identity-vault
  data:
    rest:
      base-path: /api/v1
    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
